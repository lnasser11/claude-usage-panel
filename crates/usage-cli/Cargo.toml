[package]
name = "usage-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Prints Claude Code usage from local transcripts (for verifying usage-core against real data)"

[[bin]]
name = "usage-cli"
path = "src/main.rs"

[dependencies]
usage-core = { path = "../usage-core" }
serde_json.workspace = true
chrono.workspace = true
